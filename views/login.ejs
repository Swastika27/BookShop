<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
</head>
<body>
    <form action="/login" method="POST">
        <label>Name:</label>
        <input type="text" name="name" value="<%= (typeof form != 'undefined') ? form.name:''%>" required>

        <label>Email: </label>
        <input type="email" name="email" value="<%= (typeof form != 'undefined') ? form.email:''%>" required>

        <label>Password: </label>
        <input type="password" name="password" value="<%= (typeof form != 'undefined') ? form.password:'' %>" required>

        <% if (errors.length > 0) { %>
            <ul>
                <% for (e of errors) { %>
                    <li> <%= e %> </li>
                <% } %>
            </ul>
        <% } %>

        <button type="submit">Login</button>
    </form>
    
</body>
</html>