<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>add writer</title>
    <link rel="stylesheet" href="/styleAddBook.css">
</head>
<body>
    <div class="form-container-1">
        <div class="form-btn">
            <span>Add Writer</span>
        </div>

        <form action="/publisher/writer/add" method="POST">

            <input type="text" placeholder="Writer's Name" name="writer_name" value="<%= (typeof form != undefined)? form.name :'' %>" required>
            <input type="text" placeholder="Description" name="description" value="<%= (typeof form != undefined)? form.description :'' %>">

            <div class="errors" style="color: red;">
                <% if (errors.length> 0 ) { %>
                    <% errors.forEach(err=> { %>
                        <br>
                        <%= err %>
                            <% }) %>
                                <% } %>
                    </div>
            
             <button type="submit" class="btn1">Confirm</button> 
    
        </form>

    </div>
</body>
</html> 